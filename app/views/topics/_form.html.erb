<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :title, "Give Your Topic A Nice Title" %><br>
    <%= f.text_field :title, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :body, "Explain Your Topic More" %><br>
    <%= f.text_area :body, :class => "form-control", :rows => 16 %>
  </div>
  <div class="form-group">
    <%= f.submit "Save as Draft", name: 'is_draft', class: "btn btn-info" %>
    <%= f.submit "Publish", class: "btn btn-primary" %>
  </div>
<% end %>
